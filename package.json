{
  "name": "chsplit",
  "author": "Tornith",
  "version": "0.1.1",
  "description": "CHSplit",
  "private": true,
  "dependencies": {
    "electron-is-dev": "^1.1.0",
    "js-ordered-dict": "^2.3.0",
    "open": "^7.0.0",
    "ps-tree": "^1.2.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-native": "^0.61.4",
    "react-scripts": "3.2.0",
    "request-promise": "^4.2.5",
    "socket.io": "^2.3.0",
    "socket.io-client": "^2.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "electron-dev": "concurrently \"yarn start\" \"wait-on http://localhost:3000 && electron .\"",
    "electron-pack": "electron-builder build --win portable",
    "preelectron-pack": "yarn build",
    "py-build": "pyinstaller ./pysrc/CHSplit.py -y --onefile --noconsole --name CHSplit --icon ./public/favicon.ico --distpath ./pydist --workpath ./pybuild",
    "build-dist": "yarn py-build && yarn electron-pack"
  },
  "main": "public/electron.js",
  "build": {
    "appId": "com.tornith.chsplit",
    "productName": "CHSplit",
    "buildVersion": "0.1.1",
    "files": [
      "pydist/**/*",
      "build/**/*",
      "node_modules/**/*"
    ],
    "directories": {
      "buildResources": "assets"
    }
  },
  "nsisWeb": {
    "oneClick": false,
    "perMachine": true,
    "allowToChangeInstallationDirectory": true,
    "runAfterFinish": false
  },
  "homepage": "./",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "electron": "^7.1.1",
    "electron-builder": "^21.2.0",
    "electron-react-devtools": "^0.5.3",
    "wait-on": "^3.3.0"
  }
}
